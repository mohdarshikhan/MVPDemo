<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="android:textColorTertiary">@color/textColorTertiary</item>
        <item name="android:textColorPrimaryInverse">@color/textColorPrimaryInverse</item>

        <item name="actionButtonStyle">@style/AppTheme.Widget.ActionButton</item>

    </style>

    <style name="AppTheme.Widget.ActionButton" parent="@style/Widget.AppCompat.ActionButton">
        <item name="textAllCaps">false</item>
    </style>

    <!-- Auth screens theme. -->
    <style name="AppTheme.NoTitle">
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolBarCustom">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:layout_alignParentLeft">true</item>
        <item name="android:layout_alignParentTop">true</item>
        <item name="android:background">@color/colorToolBarBackground</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="ActionBarTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/colorActionbarContents</item>
    </style>

    <style name="AllMatchParent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="AllWrap">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="MatchWrap">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="WrapMatch">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!--layout style-->
    <style name="DefaultLayout" parent="AllMatchParent">
        <item name="android:background">@color/layoutBackgroundColor</item>
    </style>

    <style name="ProgressBarAccent" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@color/progress_bar_color</item>
    </style>

    <style name="TextPrimary" parent="AllWrap">
        <item name="android:textSize">@dimen/text_small</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="HeadingText" parent="MatchWrap">
        <item name="android:padding">@dimen/small</item>
        <item name="android:background">@color/headingBGColor</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:textSize">@dimen/text_small</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="SearchEditText" parent="MatchWrap">
        <item name="android:drawableLeft">@drawable/ic_icon_search</item>
        <item name="android:drawablePadding">@dimen/small</item>
        <item name="android:padding">@dimen/small</item>
        <item name="android:minHeight">@dimen/edit_text_min_height</item>
        <item name="android:background">@drawable/rounded_search_box</item>
        <item name="android:textColor">@color/authEditTextColor</item>
        <item name="android:textSize">@dimen/text_small</item>
    </style>

</resources>
